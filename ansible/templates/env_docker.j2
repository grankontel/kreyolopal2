POSTGRES_PASSWORD='{{ pwd_postgres }}'
POSTGRES_DB='{{ db_name }}'
DB_ANON_ROLE=anon
DB_API_ROLE=postgrest
DB_SCHEMA=public
PGRST_JWT_SECRET='{{ pgrst_jwt_secret }}'

MONGO_INITDB_ROOT_PASSWORD='{{ pwd_mongo }}'
