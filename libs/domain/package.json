{
  "name": "@kreyolopal/domain",
  "version": "0.1.0",
  "description": "Shared domain elements",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "dev": "tsup --watch --config tsup.config.ts",
    "build": "tsup --config tsup.config.ts",
    "clean": "rimraf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "imports": {
    "#src/*": "./src/*.ts"
  },
  "keywords": [
    "kreyol",
    "kreyolopal"
  ],
  "author": "Thierry Malo",
  "license": "ISC",
  "devDependencies": {
    "@swc/core": "^1.4.17",
    "esbuild": "^0.20.2",
    "esbuild-node-externals": "^1.13.0",
    "npm-dts": "^1.3.12",
    "rimraf": "^5.0.5",
    "tsup": "^8.0.2",
    "typescript": "^5.4.5"
  },
  "peerDependencies": {
    "zod": "^3.23.3"
  },
  "dependencies": {
    "zod": "^3.23.3"
  }
}
